buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.1.2'
    }
}

rootProject.name = 'rush-annotation-processor'
include 'processor'
include 'annotations'
include 'runtime'

buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.1.3'
    }
}

rootProject.name = 'rush-annotation-processor'
include 'annotations'
include 'migrator'
include 'processor'
include 'runtime'
